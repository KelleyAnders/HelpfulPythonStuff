# HelpfulPythonStuff
Python code snippets I've found useful
removeblanks.ipynb imports a .csv file and turns it into a data frame and then removes any blank columns and exports it back out to a .csv
SmarterWayToLearnPython.ipynb are the exercises from the SmarterWayToLearn Python book.
SearchKWinDF.ipynb imports a csv fiole into a databframe and then search it for keywords and outputs it to another dataframe and then exports it to csv.
Interpret Results.ipynb is an exercise on regression functions. 
